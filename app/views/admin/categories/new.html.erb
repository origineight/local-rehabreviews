<h1 class="page-title">New Category</h1>

<% if @category.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@category.errors.count, "error") %> prohibited
      this category from being saved:</h2>
    <ul>
    <% @category.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="row">
  <div class="col-sm-8">
    <%= form_for @category, :html => {:class => "form-horizontal"}, url: admin_categories_path do |f| %>
      <%=render 'form', f: f %>
      <%= f.submit "Create Category", :class => 'btn btn-primary pull-right' %>
    <%end%>
  </div>
  <div class="col-md-3">
   <%= render 'members/membersidebar' %>
   <%= render 'category_sidebar' %>
  </div>
</div>
